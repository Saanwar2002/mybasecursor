
"use client";

// This file is intentionally left for renaming by the AI agent.
// Its content will be replaced by the new operational-settings page.

import { Card, CardHeader, CardTitle } from "@/components/ui/card";

export default function DeprecatedPricingSettingsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>This page is deprecated.</CardTitle>
        <p>Please use the new Operational Settings page.</p>
      </CardHeader>
    </Card>
  );
}
